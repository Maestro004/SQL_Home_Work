create table football(
                         player_name varchar ,
                         player_age int

);
insert into football(player_name, player_age) VALUES ('Nurmuhammed ' ,17);

create table flower(
                       color varchar,
                       price int
);

insert into flower (color, price) values ('red ' ,150);

create table watch (
                       size varchar,
                       gender varchar
);

insert into  watch (size, gender) values ('l','male');

create table monkeys (
                         language varchar,
                         color varchar
);


insert into monkeys (language, color) VALUES ('yaa-kuik','gray');

create table lamp (
                      color varchar,
                      figure varchar
);

insert into  lamp (color, figure) VALUES ('yellow ','kub');

create table Adyls (
                       tol int,
                       color varchar,
                       barber varchar,
                       brane varchar,
                       born varchar

);

insert into Adyls(tol, color, barber, brane, born) VALUES (150,'niger','rap','null','trava');

create table paints(
                       color varchar,
                       prise int

);

insert into paints(color, prise) VALUES ('gold',300);

create table bag (
                     size int,
                     material varchar
);

insert into bag(size, material) VALUES ('2', 'leather');

create table mess (
                      beard varchar,
                      speaks varchar
);

insert into mess(beard, speaks) VALUES ('black','much ');

create table box(
                    glass varchar,
                    color varchar
);
insert into box(glass, color) VALUES ('sun glass','transparent');








